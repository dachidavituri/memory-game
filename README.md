React Memory Game

A simple, responsive Memory (Concentration) card game built with React. Match pairs of cards by flipping them — the goal is to match all pairs using as few moves and as little time as possible.



Tech Stack:

React (Vite)
plain CSS
TypeScript


Installation
# Clone the repo
git clone https://github.com/dachidavituri/memory-game.git
cd memory-game


# Install dependencies (npm)
npm install
# or (yarn)
npm run dev